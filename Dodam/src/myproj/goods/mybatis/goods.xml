<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goods">
<!-- 상품 처음에 모든리스트 불러오는 부분 -->
<select id="goodslist" resultType="myproj.goods.dto.GoodsVO">
	select *
	from goods
	ORDER BY goods_name
</select>
<!-- 상품 검색하는 부분 -->
<select id="searchList" parameterType="String" resultType="myproj.goods.dto.GoodsVO">
	select *
	from goods
	where goods_name like '%'||#{goodsSearch}||'%'
</select>
<!-- 상품추가할 때 의약품 구분하는 부분 -->
<select id="searchmedi" parameterType="String" resultType="myproj.goods.dto.GoodsVO">
	select *
	from MEDICINE_TOTAL
	where medi_name like '%'||#{medisearchtxt}||'%'
</select>
<!-- 상품 등록하는 부분 -->
<insert id="goodsInsert" parameterType="myproj.goods.dto.GoodsVO">
 INSERT INTO goods VALUES( #{goods_name} , #{goods_date} ,#{goods_use} ,#{goods_price_in} ,#{goods_type} ,#{goods_count},#{goods_price_out})
</insert>

</mapper>